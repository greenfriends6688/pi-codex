# Spec-001：工作区 Markdown 所见即所得实时编辑

**日期：** 2026-09-12
**状态：** 需求已确认，待开发

> 需求补充（2026-09-12）：开始实现。保持现有文件面板、工具栏和 Markdown 排版，正常状态唯一可见变化是内容可放置光标并编辑；不新增编辑工具栏或保存成功提示。

## 背景

当前项目可以在工作区的文件面板中查看 Markdown 文件，并使用现有能力渲染 Markdown 内容，但还缺少符合日常写作习惯的编辑体验。

用户希望直接在工作区文件面板中编辑已有的 `.md` 文件：编辑内容时立即看到渲染效果，修改结果自动保存回原文件；如果文件被外部程序修改，系统需要实时感知并进行冲突处理。

## 目标

在现有文件查看能力上增加 Markdown 所见即所得编辑能力，满足以下核心体验：

- 点击已有 `.md` 文件后，在当前文件面板直接进入编辑状态；
- 编辑和预览在同一个单栏中完成，不使用左右分栏；
- 标题、粗体、列表等 Markdown 内容以渲染后的样式呈现，尽量隐藏源码符号；
- 用户输入后实时更新显示结果，并自动保存到原文件；
- 复用当前项目已有的 Markdown 渲染能力，不重新设计一套 Markdown 语法或预览规则；
- 检测文件的外部修改，避免静默覆盖用户正在编辑的内容。

## 需求边界

### 包含

- 编辑工作区中已经存在的 `.md` 文件；
- 单栏所见即所得编辑；
- 编辑过程中的实时渲染；
- 修改后的自动保存；
- 保存失败时显示错误，并保留当前编辑内容供用户重试；
- 文件外部修改的实时检测与冲突提示；
- 复用当前 Markdown 渲染中已有的能力，包括：
  - 标题、段落、粗体、斜体、列表、引用等基础内容；
  - 代码块；
  - Mermaid 图表；
  - 图片；
  - 本地文件链接；
  - frontmatter（文件头元数据）。
- 继续遵守当前工作区文件访问权限和路径安全规则。

### 不包含

- 新建 Markdown 文件；
- 重命名、删除或移动文件；
- `.markdown` 等其他扩展名；
- 移动端编辑适配；
- 多人协同编辑；
- 云端同步；
- 独立的 Markdown 工具栏或富文本排版系统；
- 为代码块、Mermaid、frontmatter、图片和文件链接新增独立编辑器；
- 对无法安全合并的内容自动决定采用哪一方；
- Markdown 历史版本管理。

## 交互规则

### 进入编辑

1. 用户在工作区文件面板点击已有 `.md` 文件。
2. 文件在当前文件面板中打开并进入编辑状态。
3. 不新增独立编辑页面，也不要求用户在源码模式和预览模式之间整页切换。

### 编辑与实时预览

1. 编辑器使用单栏布局。
2. 用户输入后，当前内容立即按照项目现有 Markdown 渲染规则更新。
3. 所见即所得内容应尽量显示最终样式，Markdown 标记符号只在编辑需要时出现或被弱化处理。
4. 代码块、Mermaid、图片、文件链接和 frontmatter 继续使用项目现有的显示和处理能力。
5. 编辑或保存过程中不得静默丢失上述复杂内容。

### 自动保存

1. 用户修改内容后自动保存到当前文件的原路径。
2. 自动保存可以采用短暂防抖，避免每个字符都产生一次文件写入；防抖时长属于实现参数，建议默认约 500ms。
3. 保存成功时不显示额外的成功提示。
4. 保存失败时显示明确错误信息。
5. 保存失败后，用户当前的编辑内容必须保留，不得恢复成旧文件内容，并应允许后续重试。
6. 自动保存不得创建新文件，也不得修改其他文件。

### 外部修改与冲突

1. 文件没有本地未保存修改时，如果外部程序修改了文件，编辑器应实时重新读取并更新内容。
2. 文件存在本地未保存修改时，对共同原版、本地编辑和外部版本进行三方比较，自动合并互不冲突的修改。
3. 外部更新采用短暂防抖，保留光标、选择和滚动位置；中文输入法组词期间暂缓应用更新。
4. 无法安全合并时保留双方内容并暂缓保存，仅显示不抢焦点的小提示；用户可以继续编辑，展开提示后逐个选择冲突片段采用本地或外部内容。
5. 保存前检查文件仍为已读取版本；保存失败、切换文件或重开面板时保留未保存草稿供重试。

## 验收标准

- [ ] 点击工作区中已有的 `.md` 文件后，可以在当前文件面板直接编辑。
- [ ] 编辑器为单栏布局，不出现左右分栏预览。
- [ ] 编辑时标题、粗体、列表等内容以接近最终渲染效果的样式显示。
- [ ] 修改内容后，渲染结果实时更新，不需要手动刷新或切换页面。
- [ ] 修改内容会自动保存回原 `.md` 文件。
- [ ] 重新打开该文件后，能够看到上次成功保存的内容。
- [ ] 保存失败时，界面显示错误信息，且当前编辑内容不丢失。
- [ ] 外部修改会被实时感知。
- [ ] 没有本地修改时，外部修改可以自动更新到编辑器。
- [ ] 存在本地修改时，外部修改会触发冲突提示，不会静默覆盖本地内容。
- [ ] 代码块、Mermaid、图片、本地文件链接和 frontmatter 仍按当前项目能力正常显示并在保存后保留。
- [ ] 非 `.md` 文件的现有查看行为不受影响。
- [ ] 不要求移动端支持。

## 主要场景

### 场景一：编辑并自动保存

1. 用户从文件面板打开 `notes/today.md`（工作区中的 Markdown 文件）。
2. 文件在当前面板进入所见即所得编辑状态。
3. 用户修改标题和正文。
4. 内容实时更新渲染结果。
5. 用户停止输入后，系统自动保存文件。
6. 用户重新打开文件，看到已保存的修改。

### 场景二：保存失败

1. 用户正在编辑 Markdown 文件。
2. 自动保存时发生权限、文件系统或网络请求错误。
3. 界面显示保存失败原因。
4. 用户当前编辑内容继续保留。
5. 用户修复问题后，可以再次触发自动保存或重试保存。

### 场景三：没有本地修改时发生外部修改

1. 用户打开 Markdown 文件但没有修改内容。
2. 用户通过编辑器外的程序修改该文件。
3. 系统检测到文件变化。
4. 编辑器实时加载外部最新内容。

### 场景四：存在本地修改时发生冲突

1. 用户打开 Markdown 文件并产生了未保存修改。
2. 其他程序同时修改同一个文件。
3. 系统先自动合并互不冲突的修改；仅在无法安全合并时暂缓保存。
4. 系统用不抢焦点的小提示通知冲突，同时保留用户当前编辑内容。
5. 用户展开提示，逐个选择冲突片段采用本地或外部内容，其余修改保留。

## 现有能力复用范围

| 现有模块或能力 | 在本需求中的作用 |
| --- | --- |
| `components/FileViewer.tsx`（工作区文件查看器） | 承载 Markdown 文件的打开、编辑和保存交互。 |
| `MarkdownBody`（项目现有 Markdown 渲染组件） | 作为现有 Markdown 渲染规则和表现形式的参考，避免产生不一致的预览效果。 |
| `app/api/files/[...path]/route.ts`（工作区文件接口） | 负责读取文件、写回文件、校验文件类型和执行路径访问控制。 |
| `PATCH`（文件更新请求） | 将编辑后的 Markdown 内容保存回原文件。 |
| 文件实时监听能力 | 感知外部修改并触发更新或冲突处理。 |
| Mermaid、图片、本地链接和 frontmatter 现有处理逻辑 | 保持复杂 Markdown 内容当前的渲染和打开行为。 |

## 内容保真要求

- 编辑器不得因为进入编辑状态而改写文件内容。
- 保存时必须保留未被用户修改的内容。
- 代码块、Mermaid、图片地址、本地文件链接和 frontmatter 不得因为编辑基础文本而丢失。
- 不要求第一版保证 Markdown 源码的空格、换行或标记写法完全保持不变，但不得改变用户未编辑内容的实际语义。

## 异常与安全要求

- 只允许编辑工作区允许访问范围内的已有普通文件。
- 文件不存在、不是普通文件、不是 `.md` 或无权访问时，不进入编辑状态，并显示可理解的错误信息。
- 超出当前文件读取或编辑能力限制的文件，应保持只读或提示无法编辑，不能截断后覆盖原文件。
- 保存请求失败、超时或返回异常时，不得清空编辑器内容。

## 后续测试计划

实现阶段至少补充以下测试：

- Markdown 文件进入编辑状态和退出编辑状态；
- 所见即所得内容实时更新；
- 自动保存成功与失败保留草稿；
- 外部修改的自动刷新；
- 本地修改与外部修改同时存在时的冲突保护；
- 复杂 Markdown 内容的渲染和保存后保真；
- 非 Markdown 文件行为不受影响；
- 桌面端文件面板主要编辑流程的端到端测试。

## 相关 ADR

- 暂无。当前需求是功能规格，不新增架构决策。

## 文件与术语说明

| 原名称 | 中文含义 | 用途 |
| --- | --- | --- |
| `.md` | Markdown 文件扩展名 | 本需求第一版唯一允许编辑的文件类型。 |
| WYSIWYG | 所见即所得编辑 | 编辑时尽量直接显示标题、粗体等最终样式。 |
| `frontmatter` | 文件头元数据 | Markdown 文件开头的结构化元数据内容。 |
| Mermaid | 文本生成图表的语法 | 沿用项目当前的图表渲染能力。 |
| `PATCH` | 更新请求方法 | 将 Markdown 内容写回原文件。 |
| `components/FileViewer.tsx` | 工作区文件查看器 | 当前文件面板的主要承载组件。 |
| `app/api/files/[...path]/route.ts` | 工作区文件接口 | 当前文件读取、监听和写入的服务端入口。 |

## 当前任务状态

本任务开始实现。编辑内核不自带界面，复用现有样式及复杂 Markdown 渲染；文件同步、三方合并与编辑视图分离。未编辑的原始 Markdown 片段应直接保留，避免整篇重新格式化导致额外冲突。
